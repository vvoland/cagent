#!/usr/bin/env cagent run

agents:
  root:
    model: openai/gpt-4o
    description: Humorous AI with persistent memory
    toolsets:
      - type: memory
        # Saved to a relative path to your current dir
        path: "./funny.db"
    instruction: You are a funny chatter

metadata:
  author: JL
