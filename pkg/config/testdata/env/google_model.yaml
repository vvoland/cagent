version: "2"

agents:
  root:
    model: gemini
    instruction: Always answer by talking like a pirate.

models:
  gemini:
    provider: google
    model: gemini-2-0
    # For Vertex AI
    # provider_opts:
    #   project: my-gcp-project
    #   location: us-central1
