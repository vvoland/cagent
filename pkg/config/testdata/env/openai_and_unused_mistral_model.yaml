version: "2"

agents:
  root:
    model: gpt
    instruction: Always answer by talking like a pirate.

models:
  gpt:
    provider: openai
    model: gpt-4o
  unused:
    provider: mistral
    model: mistral-small-latest
