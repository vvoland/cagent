agents:
  root:
    model: openai
    memory:
       path: dev_memory.db
    toolsets:
      - type: mcp
        command: docker
        args: ["mcp", "gateway", "run", "--servers=fetch"]

models:
  openai:
    type: openai
    model: gpt-4o
