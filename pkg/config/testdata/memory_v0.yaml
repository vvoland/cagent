version: "0"

agents:
  root:
    model: gpt
    memory:
       path: dev_memory.db
    toolsets:
      - type: mcp
        command: docker
        args: ["mcp", "gateway", "run", "--servers=fetch"]

models:
  gpt:
    type: openai
    model: gpt-4o
