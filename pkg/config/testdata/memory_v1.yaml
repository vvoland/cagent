version: "1"

agents:
  root:
    model: openai
    toolsets:
      - type: memory
        path: dev_memory.db
      - type: mcp
        command: docker
        args: ["mcp", "gateway", "run", "--servers=fetch"]

models:
  openai:
    provider: openai
    model: gpt-4o
