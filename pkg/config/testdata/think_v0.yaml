agents:
  root:
    model: openai
    think: true
    toolsets:
      - type: mcp
        command: docker
        args: ["mcp", "gateway", "run", "--servers=fetch"]

models:
  openai:
    provider: openai
    model: gpt-4o
