agents:
  root:
    name: bio
    model: openai
    todo: true
    toolsets:
      - type: mcp
        command: docker
        args: ["mcp", "gateway", "run", "--servers=brave,fetch"]

models:
  openai:
    provider: openai
    model: gpt-4o
